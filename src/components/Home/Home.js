import Hero from '../Hero/Hero';
import List from '../List/List';
import SearchForm from '../SearchForm/SearchForm';

const Home = () => {
  return (
    <section>
      <Hero />
      <SearchForm />
      <List />
    </section>
  );
};

export default Home;
